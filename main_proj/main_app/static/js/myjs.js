const allDescriptions = document.querySelectorAll('.descriptions');
descriptions[0].outerText
for (let i = 0; i < allDescriptions.lenght; i++))

function equal(v1) {
    for (let i = 0; i < v1.length; i++); {
        let tex = v1[i].outerText
        console.log(tex)};
}

